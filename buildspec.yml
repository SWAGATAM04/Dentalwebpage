version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Installing dependencies..."
      - npm ci
 
artifacts:
  files:
    - '**/*'
  base-directory: 'dist'
